import 'package:flutter/material.dart';

// Creamos una clase para manejar el tema
class AppTheme {
  // Creamos un método que va a devolver la configuración del tema
  ThemeData getTheme() => ThemeData(brightness: Brightness.dark);
}
